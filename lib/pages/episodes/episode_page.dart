import 'package:flutter/material.dart';

class EpisodePage extends StatelessWidget {
  const EpisodePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
    /*  width: 500,
      height: 500,*/
      color: Colors.blueGrey,
      child: Center(
        child: Text("Episode Page"),
      ),
    );
  }
}
